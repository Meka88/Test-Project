# Copy to .env and fill in for local experimentation.
# The Meticulous Project ID is public and lives in index.html, not here.

# Used by the mock API layer to simulate latency (milliseconds).
VITE_API_LATENCY_MS=350
